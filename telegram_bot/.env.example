# =====================================================================
# Telegram Bot Relay – variabili d'ambiente
# Copia questo file in .env e compila i valori.
# In produzione (Render) queste stesse variabili vanno impostate
# nel dashboard di Render → Environment.
# =====================================================================

# TELEGRAM_BOT_TOKEN: token segreto del bot Telegram.
#   Ottenuto da @BotFather su Telegram:
#     1. Apri Telegram e cerca @BotFather
#     2. Invia /newbot e segui le istruzioni
#     3. Al termine ricevi un token come: 1234567890:ABCDefGhIJKlmNoPQRsTUVwxyZ
TELEGRAM_BOT_TOKEN=your_bot_token_here

# RELAY_API_KEY: chiave segreta arbitraria che protegge gli endpoint REST
#   (/api/commands, /api/notify, /api/command-result) dall'accesso non autorizzato.
#   Il bot HA la invia nell'header HTTP "X-API-Key" ad ogni richiesta.
#   Deve essere identica a TELEGRAM_API_KEY nel file .env del bot HA.
#   Genera una stringa casuale, ad es.:
#     python3 -c "import secrets; print(secrets.token_hex(32))"
RELAY_API_KEY=your_secret_key_here

# ALLOWED_CHAT_IDS: lista separata da virgole degli ID Telegram degli utenti
#   autorizzati a inviare comandi al bot. Chiunque non sia in lista riceve
#   "⛔ Unauthorized".
#   Come trovare il proprio ID:
#     - Manda un messaggio a @userinfobot su Telegram
#     - Oppure usa @getidsbot
#   Esempio: 123456789,987654321
ALLOWED_CHAT_IDS=your_chat_id_here

# WEBHOOK_URL: URL pubblico di questo servizio, registrato con Telegram
#   come destinazione dei messaggi in arrivo.
#   Telegram invierà un POST a {WEBHOOK_URL}/telegram/webhook ogni volta
#   che un utente scrive al bot.
#
#   In locale: lascia vuoto → il webhook NON viene registrato e Telegram
#   non sa dove mandare i messaggi. Per testare in locale usa ngrok (vedi README).
#
#   In produzione su Render: inserire l'URL del servizio Render.
#   Esempio: https://trading-bot-telegram-relay.onrender.com
WEBHOOK_URL=
