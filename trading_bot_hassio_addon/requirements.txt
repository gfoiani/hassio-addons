# Core (pandas and numpy installed via Alpine packages in Dockerfile — no pip compilation needed)
pytz>=2024.1
requests>=2.31.0

# XTB broker (xAPI WebSocket)
websocket-client>=1.8.0

# Directa broker uses only Python stdlib (socket) – no extra dependency
